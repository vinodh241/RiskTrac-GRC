# Auto-provision Prometheus as default Grafana datasource
# Prometheus uses port 9090 (not 9000). Never use port 9000.
apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
